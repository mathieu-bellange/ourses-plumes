					<form{{= $free_log == true ? "" : " data-abide" }}>
						<fieldset>
							<legend>Authentification</legend>
							<div class="row collapse">
								<div class="column small-12">
									<input id="user_login" type="text" placeholder="Nom d&rsquo;utilisatrice ou adresse e-mail"{{= $free_log ? "" : " required pattern='alpha'" }}>
									{{? $free_log == false }}
									<small class="error">Identifiant requis&nbsp;!</small>
									{{?}}
								</div>
								<div class="column small-8">
									<input id="user_password" type="password" placeholder="Mot de passe"{{= $free_log ? "" : " required pattern='alpha'" }}>
									{{? $free_log == false }}
									<small class="error">Mot de passe requis&nbsp;!</small>
									{{?}}
								</div>
								<div class="column small-4">
									<input id="user_connection" type="button" class="connect-switch button small postfix" value="Connexion">
								</div>
							</div>
						</fieldset>
					</form>